language: python
dist: trusty
python:
  - "3.6"
  - "3.7"

install:
  - sudo apt install -y tcpdump python3
  - sudo pip3 install --pre scapy
  - sudo pip3 install -r requirements.txt

script: sudo ./test.sh
